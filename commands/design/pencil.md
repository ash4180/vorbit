---
description: Scan codebase and sync design tokens, components, and conventions to Pencil canvas — or start from scratch using Pencil style guides when no codebase exists. Builds reusable Pencil components so AI-generated mockups match project conventions.
argument-hint: [--refresh | --components-only]
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, AskUserQuestion, TaskCreate, TaskUpdate, mcp__pencil__*, mcp__mobbin__*
---

Use the **pencil** skill.

**Input:** $ARGUMENTS
