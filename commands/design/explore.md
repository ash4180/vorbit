---
description: Quick exploration of ideas before PRD creation. Generates lo-fi HTML wireframes for UI topics, with Chrome screenshot baselines when fine-tuning existing UI.
argument-hint: [topic or problem]
allowed-tools: Read, Write, Bash, Grep, Glob, AskUserQuestion, mcp__plugin_Notion_notion__*, mcp__mobbin__*, mcp__claude-in-chrome__*
---

Use the **explore** skill.

**Input:** $ARGUMENTS
