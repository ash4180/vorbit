---
description: Mine past session transcripts for learnings that were never captured
argument-hint: "[number of sessions to process, default 10]"
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Task
---

Use the **learn** skill in capture mode with the `--backfill` flag.

**Input:** --backfill $ARGUMENTS
