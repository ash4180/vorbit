interface:
  display_name: "Vorbit QA Plan"
  short_description: "Build a human-runnable QA test plan from branch specs"
  default_prompt: "Use $vorbit-qa-plan to build the QA test plan for the current branch specs."

policy:
  allow_implicit_invocation: true
